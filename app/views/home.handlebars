<h1>Home</h1>

{{#each posts}}
  <div class="post">
    <h2>{{title}}</h2>
    <p>By {{user.username}} on {{createdAt}}</p>
    <p>{{content}}</p>
    <a href="/post/{{id}}">Read More</a>
  </div>
{{/each}}
